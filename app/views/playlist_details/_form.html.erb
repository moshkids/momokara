<%= form_for(@playlist_detail) do |f| %>
  <% if @playlist_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@playlist_detail.errors.count, "error") %> prohibited this playlist_detail from being saved:</h2>

      <ul>
      <% @playlist_detail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :track_no %><br />
    <%= f.number_field :track_no %>
  </div>
  <div class="field">
    <%= f.label :track_data_id %><br />
    <%= f.number_field :track_data_id %>
  </div>
  <div class="field">
    <%= f.label :playlist_id %><br />
    <%= f.number_field :playlist_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
